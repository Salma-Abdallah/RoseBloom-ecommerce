package com.rosebloom.testing.controllers.services;

import com.rosebloom.controllers.enums.OrderStatus;
import com.rosebloom.controllers.services.CartServices;
import com.rosebloom.dtos.CartDto;
import com.rosebloom.dtos.CartIdDto;
import com.rosebloom.dtos.ProductDto;
import com.rosebloom.dtos.UserDto;
import org.assertj.core.api.Assertions;
import org.junit.jupiter.api.Test;

import java.util.List;

public class CartServicesTests {
    @Test
    void Add_Cart_Item_to_DB(){
//        CartServices cartServices = new CartServices();
//        UserDto user = new UserDto();
//        user.setId(1);
//        ProductDto product = new ProductDto();
//        product.setId(2);
//        CartIdDto cartId = new CartIdDto(product.getId(), user.getId());
//
//        CartDto cartDto = new CartDto(cartId,product,3);
//        System.out.println(cartDto);
//        cartServices.addCartItem(cartDto);
//
//        List<CartDto> cartItems =  cartServices.getAllCartItemsByUserId(user.getId());
//
//        boolean exist = false;
//        for(CartDto cart:cartItems){
////            System.out.println("cart User Id " + cart.getId().getUserId());
////            System.out.println("User Id: "+user.getId());
////            System.out.println("Cart pro id " + cart.getProduct().getId());
////            System.out.println("Pro Id "+ product.getId());
//            if(cart.getId().getUserId() == user.getId() && cart.getProduct().getId() == product.getId())exist=true;
//        }
//        Assertions.assertThat(exist).isEqualTo(true);

    }


}
